import { combineReducers } from 'redux';
import { CandidateReducer } from './CandidateReducer';

export const reducers = combineReducers({
    CandidateReducer
    //put all the reducers if there is more than one
});